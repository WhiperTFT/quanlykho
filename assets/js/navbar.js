document.addEventListener('DOMContentLoaded', function () {
    // Không cần xử lý gì ở đây nếu bạn chỉ dùng Bootstrap mặc định
    // Nếu muốn xử lý thêm custom sau khi navbar mở, bạn có thể thêm tại đây
});
